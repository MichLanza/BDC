import { RangoFechasPipe } from './rango-fechas.pipe';

describe('RangoFechasPipe', () => {
  it('create an instance', () => {
    const pipe = new RangoFechasPipe();
    expect(pipe).toBeTruthy();
  });
});
